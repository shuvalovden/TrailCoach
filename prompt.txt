Build me a personal AI coaching agent like Sisu. Here's the full spec:
WHAT IT DOES:
A Node.js server that (1) receives a webhook from Composio whenever I finish a
Strava activity, saves it to Supabase; (2) receives Telegram messages from me,
pulls my recent activity history from Supabase, sends it to Claude with a
coaching system prompt, and replies in Telegram.
TECH STACK:
- Node.js + Express
- Supabase (Postgres) via @supabase/supabase-js
- Composio for the Strava webhook (@composio/core)
- Telegram Bot API (raw fetch, no library needed)
- Anthropic Claude via @anthropic-ai/sdk (model: claude-sonnet-4-6)
- Deploy target: Railway
ENV VARS:
I've already created a .env file in this folder with these keys populated:
COMPOSIO_API_KEY, SUPABASE_URL, SUPABASE_SERVICE_KEY, TELEGRAM_BOT_TOKEN, ANTHROPIC_API_KEY,
RAILWAY_API_KEY. Read them from .env using dotenv — don't ask me for them and
don't print them back to me. Also generate a random TELEGRAM_WEBHOOK_SECRET and
add it to .env (used to verify incoming Telegram webhooks).
SECURITY:
- Create a .gitignore that excludes .env, node_modules, and .DS_Store.
- Never log API keys, tokens, or full webhook payloads at INFO level.
- Verify the Telegram webhook by checking the X-Telegram-Bot-Api-Secret-Token
header against TELEGRAM_WEBHOOK_SECRET. Reject mismatches with 401.
- Verify Composio webhook signatures if Composio supports it (check their docs
via the @composio/core SDK). If not available yet, at least check a shared
secret in the URL path (e.g. /webhook/strava/{COMPOSIO_WEBHOOK_SECRET}).
- Use the Supabase service_role key on the server only. Never expose it
client-side.
TOOLING:
Use the Supabase MCP to create the table and run migrations directly.
Use the Railway MCP to provision the project, set env vars, and deploy.
Do not make me run SQL or Railway CLI commands by hand — run them through the
MCPs.
WHAT TO BUILD:
1. An `activities` table in Supabase (create it via the Supabase MCP) with
columns:
strava_id (bigint, unique), type (text), distance_m (float),
moving_time_s (int), started_at (timestamptz), raw (jsonb), created_at (default
now()).
2. An Express server with three routes:
- POST /webhook/strava — receives the Composio payload, upserts (not inserts)
the activity into Supabase so repeated deliveries are idempotent
- POST /webhook/telegram — verifies the secret-token header, receives a
Telegram
message, calls getCoachingResponse(), replies via Telegram API. Wrap in
try/catch so a crash doesn't kill the server; respond 200 even on internal
errors (Telegram retries otherwise)
- GET /health — returns 200 OK
3. A getCoachingResponse(userText) function that:
- Pulls the user's last 14 days of activities from Supabase
- Formats them into a plain-text summary (date, type, distance in miles, time,
pace)
- Sends that summary + user message to Claude with a system prompt:
"You are an expert endurance coach. Give concrete, personalized advice based
on the athlete's recent training. Be direct and specific. Use imperial units."
- Returns Claude's reply
4. Deploy to Railway via the Railway MCP — create the project, set all 5 env vars
from the .env file, deploy, and grab the public URL.
5. Wire up the two webhooks for me (don't make me do it):
- Call Telegram's setWebhook API to point the bot at
{PUBLIC_URL}/webhook/telegram,
passing the TELEGRAM_WEBHOOK_SECRET via the secret_token parameter
(POST https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/setWebhook
body: { url, secret_token })
- Configure the Composio Strava trigger to POST to {PUBLIC_URL}/webhook/strava
(or /webhook/strava/{COMPOSIO_WEBHOOK_SECRET} if Composio can't sign yet)
using the Composio SDK or API. If that can't be done programmatically, print
the exact URL I need to paste into the Composio dashboard and where to paste
it.
6. A package.json with the right deps.
Ship the full project, run the migration via Supabase MCP, deploy via Railway MCP,
wire up the webhooks. Don't ask me clarifying questions — just build and ship.
When you're done, the app should be fully live. Tell me one thing: what Telegram
message to send to test it.